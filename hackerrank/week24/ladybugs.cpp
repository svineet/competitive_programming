#include <iostream>
#include <string>
using namespace std;

int main() {
    std::ios::sync_with_stdio(0);

    int g;
    cin >> g;
    for (int cs=0; cs<g; cs++) {
        string seq;
    }

    return 0;
}
