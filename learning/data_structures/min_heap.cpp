#include <bits/stdc++.h>
using namespace std;


class Heap {
public:
    int heap[];

    Heap(vector<int> vals, int size) {
        // Make heap out of vals
        int n = vals.size();
        heap = new int[size]; // Max size of heap is size
    }

    /*
     * Insert an element into the heap
     */
    void insert() {
    }

    /*
     * Give minimum value in heap
     */
    int peek() {
    }

    /*
     * Remove largest element and return it
     */
    int pop() {
    }
};


int main(int argc, char **argv) {

    return 0;
}
